# TODOs

Last updated: 2026-09-03 12:32:19 EDT

## Planned features

- [ ] linear mouth migration config file if possible?
- [ ] can i migrate screen settings as well (display behavior)

## Completed / fixed features

- [x] Aligned the tracked `claude/settings.json` permissions with the branch-per-feature flow and expanded the allow list to cut prompts on read-only work. The `ask` entry `Bash(git push *)` had contradicted the rule authorizing feature-branch pushes, so it was replaced by narrow entries — `git push` bare, `git push origin main*`, `git push origin HEAD*`, and both force-push spellings — while `git push origin feat/*`, `git push -u origin feat/*` and the `fix/` equivalents moved to allow. The two lists deliberately share no pattern, so the allow-versus-ask precedence for an overlap never has to be relied on; anything matching neither falls through to the default prompt. Added as allow: read-only Git (`branch` listing forms, `remote -v`, `rev-parse`, `ls-files`, `blame`, `reflog`, `stash list`, `worktree list`, `config --get`), read-only shell (`ls`, `find`, `wc`, `stat`, `file`, `head`, `tail`, `grep`, `sort`, `uniq`, `cut`, `date`, `echo`, `which`, `diff`, `jq`, `sed -n`, `df`, `du`), read-only Slurm and environment inspection (`squeue`, `sacct`, `sinfo`, `scontrol show`, `jobstats`, `module list`/`avail`, `conda env list`/`info`/`list`), and the test and lint runners (`pytest`, `python -m pytest`, `ruff`, `mypy`, `black --check`). `sbatch`, `scancel`, `srun` and `salloc` were deliberately left prompting, as were `git merge`, `rebase`, `reset`, `restore`, `checkout`, bare `stash` and `clean`. The list is 78 entries. Two known limits of prefix matching, mitigated by the sandbox write allowlist but not eliminated: `git push origin feat/x main` still matches the `feat/*` allow because the wildcard swallows the trailing ref, and `find` is the one non-read-only member of its group since `-delete` and `-exec` match `Bash(find *)`.
- [x] Moved the whole branch-per-feature Git flow into `AGENTS.md` and reduced `CLAUDE.md` back to a bare `@AGENTS.md` import, so the project file stays in sync with the global instructions instead of carrying rules of its own. This supersedes the scoping decisions in the two entries below: the flow now applies in every repository rather than only in `mac-setup`, which is what the original motivation actually needed, since the concurrent training, eval, and analysis agents work in research repositories and were getting none of it. `AGENTS.md` gained **Branch per feature, commit when done** and **Requires an explicit request** verbatim in substance from `CLAUDE.md`, while its **Multiple agents in one repository** bullets folded into the existing **Working alongside other agents** section — the merge-time recheck of the default branch as its own bullet, and the resolve-on-the-feature-branch constraint into the stop-and-ask conflict bullet. Two bullets were dropped as duplicates of **Git commits** rather than lost: batching unrelated work into one commit, and staging the `TODOs.md` update alongside the implementation. Wording was generalized for global scope — "the default branch" instead of `main`, and "the commit message rules above" instead of a cross-reference to `AGENTS.md`. Net effect on the always-loaded instructions: 151 to 184 lines.

  The authorization model was then split along the default branch rather than along the commit/push verb, resolving a contradiction the first draft introduced: **Working alongside other agents** required pushing an unfinished branch before the session ends so another machine could resume the thread, while **Requires an explicit request** gated `git push` outright. Committing to and pushing a `feat/` or `fix/` branch now need no request, since nothing shared depends on either; merging into the default branch and pushing it both do. Auto-merge was dropped in favor of proposing the merge and stopping, restoring the review checkpoint that motivated branching in the first place — the agent reaches "ready to merge" and waits. This also explains the apparent `settings.json` versus `AGENTS.md` conflict observed from the Spark machine, which was neither a conflict nor a settings bug: permissions only suppress the dialog, and the machine was running the pre-globalization `AGENTS.md` because these commits had not been pushed.

  Destructive operations were then lifted out of **Requires an explicit request** into their own **Destructive Git operations** section, on the grounds that they are a different category rather than a heavier item on the same list — the rest of that list is ordinary work awaiting authorization, while these irreversibly destroy work another branch or machine may be the only holder of. The section states that a general approval to work on a task does not cover them and neither does permission granted for one of them earlier in the session, and it requires naming what would be lost before asking. The split also removed a real duplication: `push --force` and `reset --hard` had been listed both there and in **Working alongside other agents**, whose trailing bullet is now gone. `stash`, `checkout --`, `restore`, and `clean` were added explicitly, having previously been described only as "discard or revert local changes" — `git restore` was used earlier in this same session to recover a half-applied branch switch, which the new wording would have required asking about first. Also added: deleting a branch that is unmerged or that another session created.

  Branch names then gained a thread level, `feat/<thread>/<slug>` and `fix/<thread>/<slug>`, so concurrent agents are distinguishable at a glance in `git branch` rather than only by slug. The thread set is closed at six covering the ML research lifecycle — `data`, `train`, `eval`, `analysis`, `infra`, `paper` — on the reasoning that a thread only one branch ever uses carries no coordination signal; repositories without such threads drop the level and keep `feat/<slug>`. `train` is spelled short so it reads parallel with `eval`. A rule bars naming a branch exactly `feat/<thread>`, because Git stores refs as file paths and so cannot hold `feat/train` and `feat/train/lora-rank` at once — whichever is created second is refused. `AGENTS.md` is 207 lines.
- [x] Added a **Working alongside other agents** section to `AGENTS.md` covering the cross-repository half of concurrent-session safety: `git fetch` and report ahead/behind, uncommitted, and unpushed state before starting work that changes the repository; work only on your own branch and never touch one another session created; push an unfinished branch before the session ends so another machine can resume the thread; stop and ask on any merge or rebase conflict in code, config, or job scripts, with `TODOs.md` carved out because its resolution is always to keep both sides and refresh `Last updated:`; and never `push --force`, `reset --hard`, or stash or discard local changes to make a merge apply cleanly. The split follows the scoping decision already recorded below — these rules authorize nothing, so they are safe in the globally symlinked `AGENTS.md`, while the autonomous commit-and-merge authorization stays in the project-scoped `CLAUDE.md`. The session-start fetch was the genuinely missing piece: this checkout was `ahead 2, behind 1` at the time, having missed `8d6ec56` from another device. `CLAUDE.md`'s **Multiple agents in one repository** section dropped its now-duplicated branch-ownership bullet and had its conflict rule reconciled — it had said to resolve conflicts on the feature branch, which contradicted the new stop-and-ask rule; it now states that the feature branch is *where* a conflict is resolved while asking first is still required. Branch names stay `feat/<slug>` and `fix/<slug>` rather than moving to thread-based prefixes, to avoid churning an established convention.
- [x] Added a **mac-setup project rules** section to `CLAUDE.md` authorizing a branch-per-feature Git flow in this repository only: branch off `main` as `feat/<slug>` or `fix/<slug>` before implementing, commit finished and verified work to that branch without asking, then merge with `git merge --no-ff` under an `[MRG]` subject and delete the branch. The pre-approval of `Bash(git commit *)` in `claude/settings.json` had only ever suppressed the permission dialog; nothing in `AGENTS.md` addressed *when* to commit, so the harness default of committing only on request won by silence. The rule is scoped to the project file rather than `AGENTS.md` because that file is symlinked to `~/.claude/CLAUDE.md` and `~/.codex/AGENTS.md`, where it would have opted in every repository. It is written inline instead of as an `@` import because nested imports silently fail to expand. Push, pull requests, history rewriting, and committing unverified or unrelated pre-existing work still require an explicit request, and a **Multiple agents** subsection keeps concurrent sessions off each other's branches.
- [x] Moved the bulk of the FASRC Cannon / Kempner reference out of `AGENTS.md` into a tracked `fasrc` skill at `claude/skills/fasrc/SKILL.md`, keeping only the lab paths and a commented sample `#SBATCH` header inline because those are small and must never be guessed. The skill holds the partition guide, per-GPU core and memory caps, GPU holding limits, requeue constraints, Python environment setup, the full job template, the `$HOME` path shortcuts, and the Slurm monitoring commands. The always-loaded instructions dropped from 10,844 to 7,383 bytes, roughly 865 tokens saved in every session in every project, while an agent that never loads the skill still has the account, the paths, and an explicit instruction not to fill in a partition or resource figure from memory. All three routing cases point at the skill, and `setup-hpc.sh` now warns on a FASRC login node when the skill is not readable.
- [x] Moved the handoff document specification out of `AGENTS.md` into a tracked Claude Code skill at `claude/skills/handoff/SKILL.md`, and rewrote it to capture the live session thread — task in flight, decisions and rationale, open questions — alongside the existing project-state sections, so a handoff can carry work to another agent, another harness, or another person rather than only summarizing finished work for a human. `link-agent-docs.sh` gained a `link_skills` helper that symlinks each tracked skill directory into `~/.claude/skills/<name>` individually, leaving room for skills installed from elsewhere and refusing to replace a real directory. Removing the section also dropped 13 lines from the always-loaded `AGENTS.md`.
- [x] Completed the Kempner per-GPU scheduling caps in `AGENTS.md` with the previously missing `kempner_h200` (16 cores, 360 GB) and `kempner_rtx` (16 cores, 180 GB) rows, so every Kempner GPU partition in the partition guide now has a documented `-c` and `--mem` ceiling instead of only `kempner` and `kempner_h100`. The four partitions are now a table rather than bullets, and the GPU holding limits state that the 16-per-user and 96-per-account caps are concurrent and summed across those four partitions.
- [x] Added a **Path Shortcuts** subsection to the FASRC reference in `AGENTS.md`, documenting the `~/lab` and `~/scratch` symlinks to the holylabs and netscratch lab directories, the matching `cdlab` and `cdscratch` aliases, and the caveat that `#SBATCH` directives and job scripts still need the full paths because Slurm does not expand `~` and aliases do not exist in job shells.
- [x] Added a **Checking Jobs and Resource Usage** subsection to the FASRC Slurm reference in `AGENTS.md`, documenting `squeue --me` for pending and running jobs, `sacct` for finished-job state and memory high-water mark, and `jobstats <job_id>` for requested versus used CPU and memory when right-sizing `-c` and `--mem` before resubmitting.
- [x] Pre-approved `pdftotext` (both bare and argument forms) in the tracked `claude/settings.json`, so the `poppler` PDF extraction already installed by `Brewfile` runs without a permission prompt on every machine receiving the symlinked global settings.
- [x] Pre-approved Claude Code `WebFetch` and `WebSearch` in the tracked `claude/settings.json`, so every machine receiving the symlinked global settings has the same web-access permissions.
- [x] Corrected the FASRC job I/O, checkpoint, and log path in `AGENTS.md` and its sample Slurm script by adding the missing `Lab` directory, so they use the writable `/n/netscratch/schung_lab/Lab/xizheng/` path; `CLAUDE.md` and the global Claude instructions inherit the fix from `AGENTS.md`.
- [x] Symlinked the tracked `claude/settings.json` into `~/.claude/settings.json` from every per-device setup script and the Mac bootstrap, using the shared idempotent link helper so `git pull` propagates settings changes automatically while preserving an existing regular file once as `settings.json.pre-mac-setup`.
- [x] Merged the FASRC Cannon / Kempner reference into `AGENTS.md` as an inline section and deleted the separate `HPC.md`, so the cluster paths, accounts, partitions, Slurm options, and environment rules load once per session instead of needing an on-demand read that a context compaction can drop. All content was carried over verbatim. `link-agent-docs.sh` no longer links `HPC.md` and now removes the stale `~/.claude/HPC.md` symlink it previously installed, while leaving a hand-kept real file at that path untouched. The obsolete `Read(~/.claude/HPC.md)` pre-approval was dropped from `claude/settings.json`.
- [x] Pre-approved the environment detection commands (`uname`, `command -v`, `nvidia-smi`, in both bare and argument forms) in the tracked `claude/settings.json`, so the compute environment routing rules can identify Mac, Spark, or FASRC in headless `-p` runs where a denied permission prompt previously blocked detection.
- [x] Replaced the broken `@` import of the HPC reference in `AGENTS.md` with an explicit instruction to read `~/.claude/HPC.md` before naming any partition, account, `#SBATCH` option, cluster path, or environment, and pre-approved `Read(~/.claude/HPC.md)` in the tracked `claude/settings.json`. Spark testing showed the nested import never expanded — the same was true in-repo on the Mac with no symlink involved — so Claude knew heavy jobs route to FASRC but could not name a partition or account. An earlier attempt to fix this by switching to the home-relative `@~/.claude/HPC.md` path did not work, because the path was never the cause.
- [x] Reported unified memory in `setup-spark.sh` using the total system memory when `nvidia-smi` gives no discrete VRAM figure, so GB10 hardware shows a usable local memory bound instead of `[N/A]`.
- [x] Added per-device setup scripts (`setup-mac.sh`, `setup-spark.sh`, `setup-hpc.sh`) that each refuse to run on the wrong machine and delegate to a shared `link-agent-docs.sh`, which symlinks `AGENTS.md` and `HPC.md` into `~/.claude/` and `~/.codex/` so the instructions apply in every project and `git pull` is the only update step. `bootstrap.sh` remains a Mac-only accessory installer.
- [x] Replaced the single-line HPC pointer in `AGENTS.md` with compute environment routing rules that detect Mac, Spark, or FASRC and define where work may run in each case: light edits and CPU-light checks only on the Mac, sub-15-minute GPU debug runs on Spark, heavy or fan-out jobs routed to FASRC `sbatch` with permission, and `HPC.md` followed directly on the cluster.
- [x] Updated the default Homebrew desktop app set to add Hidden Bar and Tailscale while no longer installing Docker or MotrixNext.
- [x] Added reusable SSH aliases for the Spark internal and Tailscale addresses.
- [x] Disabled the VS Code Secondary Side Bar's default visibility so Chat no longer opens automatically in new windows or workspaces.
- [x] Added Microsoft's Python extension to bootstrap setup so VS Code Python support and the tracked Python editor settings work on a new Mac.
- [x] Replaced the floating placeholder Miniforge download with the pinned `26.5.3-0` release and versioned macOS installer filename.
- [x] Added the Microsoft Remote - SSH VS Code extension to bootstrap setup so restored SSH hosts appear in Remote Explorer on a new Mac.
- [x] Preserved the global Claude Code `settings.json` under `claude/` and added safe bootstrap restoration to `~/.claude/settings.json` without migrating credentials, sessions, or project history.
- [x] Added the `poppler` formula to `Brewfile` so `pdftotext` and related PDF utilities used by AI PDF reading are installed by default.
- [x] Documented a reusable Oh My Zsh optimization that avoids duplicate NVM and eager runtime initialization while preserving custom paths, Java, Conda access, and aliases.
- [x] Added a repository-wide Git ignore rule for macOS `.DS_Store` metadata files.
- [x] Documented the manual ChatGPT/Codex and Claude connector setup for Notion, Gmail, Google Calendar, and local Obsidian vault access without storing authentication data.
- [x] Replaced the Anaconda Homebrew cask with an idempotent official Miniforge installation, disabled automatic base activation, and added conda/mamba verification.
- [x] Saved the reusable SSH host configuration and added permission-safe bootstrap restoration without copying keys or `known_hosts`.
- [x] Preserved the current VS Code user settings under `vscode/` and added safe bootstrap restoration without migrating extensions.
- [x] Moved manual Xcode CLI Tools, Homebrew, Brewfile, and optional-app instructions into a dedicated guide linked from the README.
- [x] Added an idempotent macOS `bootstrap.sh` for Xcode CLI Tools, Homebrew, Brewfile installation, and command verification.
- [x] Added a declarative `Brewfile` for the standard Mac setup while keeping optional applications outside the default installation.
- [x] Switched Node.js setup from NVM to a system-level Homebrew installation with verification commands.
- [x] Added shared repository instructions for Codex and Claude Code through `AGENTS.md` and `CLAUDE.md`.
- [x] Documented separate Homebrew installs for the ChatGPT and Claude desktop apps and the Codex and Claude Code CLIs.
- [x] Added a versioned inventory of the currently installed and enabled Obsidian community and core plugins.
- [x] Made TODO maintenance optional for routine changes unless `TODOs.md` already exists or the user explicitly requests tracking.
